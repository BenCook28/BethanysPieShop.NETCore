@model IEnumberable<BethanysPieShop.Models.Pie>

<html>
<head>
    <title>Bethany's Pie Shop</title>
</head>
<body>
    @ViewBag.CurrentCategory
    @foreach (var item in Model)
    {
        <div>
            <h2>@pie.Name</h2>
            <h3>@pie.Price.ToString("c")</h3>
            <h4>@pie.Category.CategoryName</h4>
        </div>
    }
</body>
</html>